{
  "name": "domain-sites",
  "version": "0.1.0",
  "private": true,
  "description": "Five static, multilingual landing sites (TR/EN/AR) served from one repository by hostname.",
  "type": "module",
  "scripts": {
    "dev": "node --watch tools/serve.mjs",
    "build": "node tools/build.mjs",
    "check": "node tools/check.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
